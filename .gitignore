# Build output
out/
*.o
*.ko.cmd

# Accidentally-cloned reference repos (this has happened before —
# see README's debugging history. Clone reference trees OUTSIDE this
# device tree's directory, not inside it.)
mt6789-common/

# OS/editor cruft
.DS_Store
Thumbs.db
*.swp
*~

# Local scratch space some contributors may use
scratch/
*.tmp
